import React from "react";
import Navbar from "./Navbar";

const Header = () => {
  <header className=" text-gray-600 flex justify-between items-center">
    {/* <button onClick={toggleSidebar} className="text-white">
      Menu
    </button> */}
    <Navbar />
  </header>;
};

export default Header;
